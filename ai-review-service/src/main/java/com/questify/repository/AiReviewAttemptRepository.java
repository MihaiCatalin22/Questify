package com.questify.repository;

import com.questify.domain.AiReviewAttempt;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AiReviewAttemptRepository extends JpaRepository<AiReviewAttempt, Long> {
}
