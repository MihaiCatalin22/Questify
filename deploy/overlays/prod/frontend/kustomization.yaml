apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default

resources:
  - ../../../base/frontend
  - ingress-tailscale.yaml

patches:
  - path: patch-nginx.yaml

configMapGenerator:
  - name: nginx-config
    files:
      - default.conf

generatorOptions:
  disableNameSuffixHash: true

images:
  - name: ghcr.io/mihaicatalin22/questify-frontend
    newTag: feat-cicd2-3c912a7
