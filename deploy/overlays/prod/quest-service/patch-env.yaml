apiVersion: apps/v1
kind: Rollout
metadata:
  name: quest-service
  namespace: default
spec:
  template:
    spec:
      containers:
        - name: quest-service
          env:
            - name: SUBMISSION_SERVICE_BASE
              value: http://submission-service/api
            - name: INTERNAL_TOKEN
              valueFrom:
                secretKeyRef:
                  name: internal-token
                  key: token
            - name: SECURITY_INTERNAL_TOKEN
              valueFrom:
                secretKeyRef:
                  name: internal-token
                  key: token
