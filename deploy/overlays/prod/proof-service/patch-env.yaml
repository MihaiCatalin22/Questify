apiVersion: apps/v1
kind: Deployment
metadata:
  name: proof-service
  namespace: default
spec:
  template:
    spec:
      containers:
        - name: proof-service
          env:
            - name: SECURITY_INTERNAL_TOKEN
              valueFrom:
                secretKeyRef:
                  name: internal-token
                  key: token
