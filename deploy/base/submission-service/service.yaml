apiVersion: v1
kind: Service
metadata:
  name: submission-service
  labels:
    app: submission-service
spec:
  type: ClusterIP
  selector:
    app: submission-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
