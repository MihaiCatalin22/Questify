apiVersion: v1
kind: Service
metadata:
  name: quest-service
  labels:
    app: quest-service
spec:
  type: ClusterIP
  selector:
    app: quest-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
