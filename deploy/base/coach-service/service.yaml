apiVersion: v1
kind: Service
metadata:
  name: coach-service
  labels:
    app: coach-service
spec:
  type: ClusterIP
  selector:
    app: coach-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
